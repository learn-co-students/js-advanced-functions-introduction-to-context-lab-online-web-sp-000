function createEmployeeRecord(array) {
    let result = array.map(obj => {
        let newobj = {}
        console.log(newobj)
        let timeInEvents = []
        let timeOutEvents = []
    })
    
        
}